<h1>Awards</h1>

<table class="list">
  <tbody>
    <% @awards.each do |award| %>
      <tr>
        <td><%= link_to award.title, edit_admin_award_path(award) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
