<h1>Pages</h1>

<table class="list">
  <tbody>
    <% @pages.each do |page| %>
      <tr>
        <td><%= link_to page.title, edit_admin_page_path(page) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
